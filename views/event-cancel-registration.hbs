<h1>You are no longer registered to this event!</h1>
<h2>We hope to see you at another event soon.</h2>
<br>
<h3>{{ data.title }}</h3>
<h4>{{ data.type }}</h4>
<ul>
  <li>{{ data.date }} - {{ data.location }}</li>
</ul>
<br>

{{!-- <img class="event-picture"
     src="{{ eventsData.picture }}"
     alt= "{{ eventsData.title }} picture" > --}}

<a href="/events">
  <button type="submit" class="btn btn-primary">All Events</button>
</a>